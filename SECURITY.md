# Security

## 信頼モデル

`reaperctl`は任意のLuaをREAPERプロセス内で実行します。LuaはREAPERと同じユーザー権限を持ち、Lua標準ライブラリとREAPER APIを利用できます。このプロジェクトはサンドボックス、認証境界、マルチユーザー実行基盤ではありません。

次の条件を満たす環境だけで使用してください。

- 内容を確認したLuaだけを実行する
- IPCディレクトリをローカルに置く
- IPCディレクトリへの書き込みを信頼できるユーザーとプロセスに限定する
- ネットワーク共有や不特定ユーザーが書けるディレクトリを使わない
- 重要なREAPERプロジェクトは保存・バックアップしてから編集する

IPCディレクトリへ書き込める第三者は、要求ファイルを作成して任意コードを実行できます。IDと有効期限は要求の対応付けと古い要求の拒否に使うもので、認証機能ではありません。

## 運用上の注意

CLIのtimeoutはREAPER内で開始済みのLuaを停止しません。timeoutや中断後に同じ編集を再送すると二重実行になる可能性があります。必ずREAPERの状態を再観測してください。

Lua実行の途中で失敗しても、REAPERへの変更は自動ロールバックされません。編集スクリプトではUndoブロックを使い、実行後の状態を検証してください。

## 脆弱性の報告

公開Issueへ認証情報、個人情報、未公開プロジェクト、悪用可能な詳細を投稿しないでください。GitHubリポジトリでSecurity Advisoriesが利用可能な場合はPrivate vulnerability reportingから報告してください。利用できない場合は、機密情報を除いた最小限の再現条件でmaintainerへ連絡してください。
